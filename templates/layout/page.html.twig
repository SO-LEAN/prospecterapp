{% extends 'layout/base.html.twig' %}
{% block body %}
    <div class="container-fluid">
        <div class="row">
            <nav class="col-sm-2 bg-light sidebar">
                <div class="sidebar-sticky">
                    {{ knp_menu_render('secondary') }}
                </div>
            </nav>
            <div class="col-sm-8">
                {% block page_body %}

                {% endblock %}
            </div>
            <div class="col-sm-2">
                {% block right %}

                {% endblock %}
            </div>
        </div>
    </div>

{% endblock %}
